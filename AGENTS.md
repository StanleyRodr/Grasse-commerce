# Repository Instructions

- This directory currently contains no application files, manifests, scripts, tests, or Git metadata.
- Do not assume a framework, package manager, build command, or test command; establish those when the project is added.
